apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-app-frontend-deployment
spec:

  replicas: 3
  template:  # This block represents the pod template; the pods to be managed by this Deployment.
    metadata:
      labels:
        app: sample-app-frontend-pods  # The pods will be created with the label app: sample-app-pods
    spec:
      containers:
        - name: sample-app-frontend
          image: sample-node-app-frontend:0.1
          # image: <Image URI if in ECR>
          ports:
            - containerPort: 8080
          env:
            - name: NODE_ENV
              value: production
          imagePullPolicy: Never
  selector:
    matchLabels:
      app: sample-app-frontend-pods  # ie. This deployment is managing pods that have the label app: sample-app-pods
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 3
      maxUnavailable: 0
