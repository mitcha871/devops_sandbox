In this folder we enable testing of IaC code in AWS by allowing Github to authenticate to AWS via OIDC.

We also create a S3 bucket to use as a remote backend.

Then we create an additional IAM role which applies infrastructure changes has write permissions).
